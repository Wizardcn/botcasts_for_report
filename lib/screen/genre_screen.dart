import 'package:flutter/material.dart';

class MyGenre extends StatelessWidget{
  const MyGenre({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(

      ),
    );
  }
}